<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:KSW.UI.WPF.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/KSW.UI.WPF;component/Icons/Icon.xaml"></ResourceDictionary>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:ClosableTag}">
        <Setter Property="VerticalAlignment" Value="Stretch"></Setter>
        <Setter Property="Foreground" Value="{DynamicResource KSW.Text.Color0}"></Setter>
        <Setter Property="Background" Value="{DynamicResource KSW.Background.Color4}"></Setter>
        <Setter Property="BorderBrush" Value="{DynamicResource KSW.ClosableTag.Brush.Border}"></Setter>
        <Setter Property="BorderThickness" Value="1"></Setter>
        <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ClosableTag}">
                    <Border
                            Margin="1"
                            Padding="8,4,4,4"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <DockPanel LastChildFill="True">
                            <Button x:Name="PART_CloseButton"
                                    DockPanel.Dock="Right"
                                    Width="12"
                                    Height="12"
                                    Margin="4,0,0,0"
                                    Style="{DynamicResource InnerIconButton}"
                                    Command="{x:Static controls:ClosableTag.CloseCommand}"
                                    Content="{DynamicResource KSW.Icon.Close}">
                            </Button>
                            <ContentPresenter 
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                TextElement.FontSize="12"
                                TextElement.Foreground="{TemplateBinding Foreground}"
                                ></ContentPresenter>
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>