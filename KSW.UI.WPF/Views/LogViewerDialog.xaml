<UserControl x:Class="KSW.UI.WPF.Views.LogViewerDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KSW.UI.WPF.Views"
             xmlns:prism ="http://prismlibrary.com/"
             xmlns:vm="clr-namespace:KSW.UI.WPF.ViewModels"
             xmlns:language="clr-namespace:KSW.UI.WPF.Language"
             xmlns:converters="clr-namespace:KSW.UI.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:LogViewerDialogViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <converters:NullableToVisibilityConverter x:Key="NullableToVisibilityConverter"></converters:NullableToVisibilityConverter>
    </UserControl.Resources>

    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="WindowStyle" Value="ThreeDBorderWindow"/>
            <Setter Property="Width" Value="800"></Setter>
            <Setter Property="Height" Value="600"></Setter>
            <Setter Property="Icon" Value="{x:Null}"></Setter>
        </Style>
    </prism:Dialog.WindowStyle>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Orientation="Horizontal" Margin="4">
            <ComboBox Width="100" Margin="2"
                      SelectedValue="{Binding SelectedLogLevel}"
                      SelectedValuePath="Content"
                      HorizontalContentAlignment="Center"
                      Padding="6"
                      Style="{DynamicResource MaterialDesignOutlinedComboBox}">
                <ComboBoxItem Content="{language:L All}" />
                <ComboBoxItem Content="Debug"/>
                <ComboBoxItem Content="Information"/>
                <ComboBoxItem Content="Warning"/>
                <ComboBoxItem Content="Error"/>
            </ComboBox>
            <TextBox Text="{Binding FilterKeyword, UpdateSourceTrigger=PropertyChanged}"
                     Width="200" Margin="2" ToolTip="{language:L SearchKeyword}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="4" VerticalContentAlignment="Center"/>
            <Button Content="{language:L Clear}" Command="{Binding ClearLogsCommand}" Margin="2" Padding="8,2"/>
            <Button Content="{language:L Export}" Command="{Binding ExportLogsCommand}" Margin="2" Padding="8,2" Style="{DynamicResource MaterialDesignFlatButton}"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                 ItemsSource="{Binding DisplayedEntriesView}"
                 AutoGenerateColumns="False"
                 CanUserAddRows="False"
                 CanUserDeleteRows="False"
                 CanUserResizeRows="False"
                 CanUserSortColumns="False"
                 IsReadOnly="True"
                 SelectionMode="Single"
                 SelectionUnit="FullRow"
                 VerticalAlignment="Stretch"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="DetailsVisibility" Value="{Binding Exception,Converter ={StaticResource NullableToVisibilityConverter}}"></Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns> 
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat='{}{0:yyyy.MM.dd HH:mm:ss.fff}'}" Width="Auto" />
                <DataGridTemplateColumn Header="Level" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="3" Padding="7,2" HorizontalAlignment="Center" Margin="0,1" BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding LevelEnum}" Value="Warning">
                                                <Setter Property="BorderBrush" Value="{DynamicResource KSW.Toast.Brush.Success}"></Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding LevelEnum}" Value="Information">
                                                <Setter Property="BorderBrush" Value="{DynamicResource KSW.Toast.Brush.Information}"></Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding LevelEnum}" Value="Warning">
                                                <Setter Property="BorderBrush" Value="{DynamicResource KSW.Toast.Brush.Warning}"></Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding LevelEnum}" Value="Error">
                                                <Setter Property="BorderBrush" Value="{DynamicResource KSW.Toast.Brush.Error}"></Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding LevelEnum}" Value="Fatal">
                                                <Setter Property="BorderBrush" Value="{DynamicResource KSW.Toast.Brush.Fatal}"></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Level}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding LevelEnum}" Value="Debug">
                                                    <Setter Property="Foreground" Value="{DynamicResource KSW.Toast.Brush.Success}"></Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LevelEnum}" Value="Information">
                                                    <Setter Property="Foreground" Value="{DynamicResource KSW.Toast.Brush.Information}"></Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LevelEnum}" Value="Warning">
                                                    <Setter Property="Foreground" Value="{DynamicResource KSW.Toast.Brush.Warning}"></Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LevelEnum}" Value="Error">
                                                    <Setter Property="Foreground" Value="{DynamicResource KSW.Toast.Brush.Error}"></Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LevelEnum}" Value="Fatal">
                                                    <Setter Property="Foreground" Value="{DynamicResource KSW.Toast.Brush.Fatal}"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Source" Binding="{Binding SourceContext}" Width="Auto"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="Auto"/>
            </DataGrid.Columns>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <TextBlock Margin="20,0,0,0" Text="{Binding Exception}" TextWrapping="Wrap"/>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
        <!-- 日志列表 -->
        <!--<ListBox Grid.Row="1"
                 ItemsSource="{Binding DisplayedEntriesView}"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 SelectedValue="{Binding SelectedLog}">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{language:L Copy}" Command="{Binding CopyCommand}" ></MenuItem>
                </ContextMenu>
            </ListBox.ContextMenu>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock x:Name="DisplayTB">
                        <Run Text="{Binding Timestamp, StringFormat='[{0:HH:mm:ss.fff}]'}"
                             Foreground="Gray"/>
                        <Run Text="{Binding Level, StringFormat=' [{0}]'}"
                             Foreground="{Binding Level}"/>
                        <Run Text="{Binding Message}"/>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->

        <!-- 状态栏 -->
        <StatusBar 
            Grid.Row="2" 
            Background="{DynamicResource KSW.StatusBar.Background.Color}" 
            BorderBrush="{DynamicResource KSW.StatusBar.BorderBrush.Color}"
            BorderThickness="0,1,0,0"
            Padding="12,5">
            <TextBlock Text="{Binding StatusText}" FontSize="11"/>
        </StatusBar>

    </Grid>


</UserControl>
